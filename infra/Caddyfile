# Замените api.proev.ru на реальный поддомен, указывающий A-записью на IP вашего VPS.
# Caddy автоматически выпустит и обновит HTTPS-сертификат (Let's Encrypt) — ничего вручную настраивать не нужно.

api.proev.ru {
    reverse_proxy backend:3001
}
